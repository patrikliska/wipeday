# Screen: `help`

**Command:** `/help`. **Visibility:** ephemeral. **Card:** none.

## Purpose
Rule 6 says `/help` must exist but must never be needed. It is three lines: what the game
is, that everything happens on the base message, and the three verbs (Collect, Gather,
Tools). No button rows: it is a note, not a screen with actions.
